#include <iostream>
#include <fstream>
#include <vector>

using namespace std;

/**
 * @brief Function to write a matrix to a CSV file
 *
 * @param[in] mat - The matrix to write to the CSV file
 * @param[in] filename - The name of the file to write to
 */
void writeMatrixToCSV(vector<vector<double>> mat, string filename);
